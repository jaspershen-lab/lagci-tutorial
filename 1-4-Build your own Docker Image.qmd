# Build your own Docker Image

You can customize your own docker image based on `lagci` or `lagcishiny` docker.

If you do not familiar with build docker, you can check this [LINK](https://devopscube.com/build-docker-image/) to realize the command in `Dockerfile`.

## Session information

```{r,eval=TRUE,warning=FALSE, R.options="", message=FALSE, cache=TRUE}
sessionInfo()
```
